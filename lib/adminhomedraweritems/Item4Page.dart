import 'package:flutter/material.dart';

class Item4Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Route Management'),
      ),
      body: Center(
        child: Text('This is Item 4 Page'),
      ),
    );
  }
}