
import 'package:flutter/material.dart';

class Container3Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Container 3 Page'),
      ),
      body: Center(
        child: Text('This is Container 3 Page'),
      ),
    );
  }
}