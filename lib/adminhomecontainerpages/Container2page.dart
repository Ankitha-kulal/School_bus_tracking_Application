
import 'package:flutter/material.dart';

class Container2Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Container 2 Page'),
      ),
      body: Center(
        child: Text('This is Container 2 Page'),
      ),
    );
  }
}