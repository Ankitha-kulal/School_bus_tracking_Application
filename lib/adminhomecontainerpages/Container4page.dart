
import 'package:flutter/material.dart';

class Container4Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Container 4 Page'),
      ),
      body: Center(
        child: Text('This is Container 4 Page'),
      ),
    );
  }
}
